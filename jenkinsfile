@Library('Shared-Library@master') _

node(){
  
	
		stage('Checkout Input') {
			                        //git checkout
			                       gitCheckout(branch: "master",url: "https://github.com/punnammani/GroovyPractice.git")
		}	
	
		
		   stage('Analyze & Report') {
			
			
			
			                          bat 'dir'
			                          //extracting values from CSV file and then making report
                                                  extractValuesFromCSV()
                  }
		  
		  
		  stage('Email Notification') {
			
		
	                                           //send out an email notification to recipient 
                                                   emailNotification()
                  }
}
