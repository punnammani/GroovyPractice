@Library('Shared-Library@master') _

pipeline {
  agent any 
	
    stages {
	
		stage('Checkout') {
			steps {
			 gitCheckout(
				branch: "master",
				url: "https://github.com/punnammani/GroovyPractice.git"
				)
			}
		}
		
		stage('Analyze&Report') {
			steps {
			
			//sh 'pwd'
			//sh 'ls'
			bat 'dir'
			println("$WORKSPACE")

			
			//extractValuesFromCSV()
			jenkinscsv()
			
			}
		}	
		
	}
	
	post {
  always {
    emailNotification()
  }
}
}
